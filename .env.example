ANTHROPIC_API_KEY=
BACKEND_URL=http://localhost:8000

# Leave unset for local development: the backend falls back to a SQLite file
# at backend/data/chat.db. For deployment, point this at hosted Postgres (Neon):
# DATABASE_URL=postgresql+psycopg://USER:PASSWORD@HOST/DBNAME?sslmode=require
